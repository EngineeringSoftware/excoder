Please edit the given Java method, `~`mut_name`~` in class `~`class_name`~`,  so that it can throw `~`exceptions`~` and pass the given exceptional_behavior test. 

The original method that needs throw statement is defined as the following: 

~`editor@mut_no_throw:coverage,unre,thrown-exception`~

The exceptional behavior tests that you must pass is:
~`etests`~~`throw-info`~~`nebt`~~`class-info`~~`method-info`~

The tentative implementation you need to help fix is 

```java
~`tent_code`~
```

The error I'm getting is:

~`error_info`~

Return **ONLY** the modified method with all throw statements added. Please do not reply multiple modified methods. Please return the **FULL** method with added throw statement.
