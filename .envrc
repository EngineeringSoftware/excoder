# Optional: for direnv users who want the repository paths and the virtualenv in
# every shell.  The four pipelines in scripts/ do not need this -- they source
# scripts/helper/env.sh themselves, so they work from any directory with or
# without direnv.
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/scripts/helper/env.sh"
